#ifndef MACSURF_STAT_H
#define MACSURF_STAT_H
/* CodeWarrior provides struct stat and S_IF macros natively.
   This shim is intentionally blank to prevent redefinition crashes
   while still satisfying #include <stat.h> directives in NetSurf. */
#endif
