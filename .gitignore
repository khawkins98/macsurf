# Private / non-public — never commit
.private/
.claude/
CLAUDE.md
GEMINI.md
.gemini/

# Build archives & fix tars (these go to .private/archives/)
*.zip
*.tar
*.tar.gz
*.tgz
*.sit
*.hqx

# Build artifacts
MacSurf
MacSurf.xml
*.o
*.a
*.lib

# Runtime logs
*.log
MacSurf Debug.log
StdLog

# macOS resource-fork metadata
._*
.DS_Store

# Scratch error logs
errors*.txt
errrors*.txt
newerrors*.txt
newerrors*txt
build-error-*.txt

# Node / Gemini CLI dependencies (moved to .private)
node_modules/
package.json
package-lock.json

# Legacy / scratch build trees
clean_macsurf/
flat_macsurf_sources/
tree_macsurf/
build/
builds/
for-gemini/

# Misc scratch
text.txt
test_ending.txt
test_pragma.c
animation.as
forgem/

# macSSL is its own git repo at macSSL/ — manage separately (submodule or merge later)
macSSL/
